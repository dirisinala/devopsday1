pipeline{
    agent any
    stages{
        stage ('Build')
        {
            steps{
                echo 'first build'
		sh 'docker build -t test1.0 .'	    
		    echo 'first build'
		    echo 'first build'
            }
        }
            stage ('Deploy')
        {
            steps{
                echo 'first build'
            }

        }
    }
    
}
